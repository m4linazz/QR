<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Генератор QR-кода</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: #fff;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow: hidden;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            text-align: center;
        }

        form {
            background: rgba(255, 255, 255, 0.1);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(8px);
            width: 100%;
            max-width: 500px;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            align-items: center;
        }

        label {
            font-size: 1.2rem;
            font-weight: bold;
        }

        input[type="text"], input[type="color"] {
            padding: 0.8rem;
            width: 90%;
            max-width: 400px;
            border: none;
            border-radius: 6px;
            font-size: 1rem;
            outline: none;
            box-shadow: inset 2px 2px 8px rgba(0, 0, 0, 0.2);
        }

        button {
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: white;
            font-size: 1.2rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
        }

        button:hover {
            transform: scale(1.05);
            box-shadow: 0px 6px 8px rgba(0, 0, 0, 0.4);
        }

        h2 {
            font-size: 2rem;
            margin-top: 2rem;
            text-align: center;
        }

        img {
            margin-top: 1rem;
            max-width: 300px;
            width: 100%;
            height: auto;
            border: 4px solid rgba(255, 255, 255, 0.5);
            border-radius: 12px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>
    <h1>Генератор QR-кода</h1>
    <form action="/generate" method="POST">
        <label for="text">Введите текст или URL:</label>
        <input type="text" id="text" name="text" required>

        <label for="darkColor">Выберите цвет темных элементов:</label>
        <input type="color" id="darkColor" name="darkColor" value="#000000">

        <label for="lightColor">Выберите цвет светлых элементов:</label>
        <input type="color" id="lightColor" name="lightColor" value="#ffffff">

        <button type="submit">Сгенерировать QR-код</button>
    </form>

    <% if (qr) { %>
        <h2>Ваш QR-код:</h2>
        <img src="<%= qr %>" alt="QR code">
    <% } %>
</body>
</html>
